#you may need to
#install.packages('servr')
library(servr)

#Change this to the directory your "index.html" is in
rootDir <- paste0('c:/users/niacj1/Documents/LectureINST728Q/')

httd(rootDir);

